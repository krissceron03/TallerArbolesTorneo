package Clases;

public class PruebArbol {
   /*static int sumaClaves (nodoArbol arbol) {
        int resul;
        if (arbol != null){
            resul = arbol.getClave() + sumaClaves (arbol.getIzq()) + sumaClaves (arbol.getDer()) ;
        }
        else resul = 0;
        return resul;
    }

    static int sumaClaves (arbol a) {
        return sumaClaves (a.getRaiz());
    }
    static int cuentaHojas (nodoArbol arbol) {
        int resul = 0;
        if (arbol != null)
            if ((arbol.getIzq() == null) && (arbol.getDer() == null))
                resul = 1;
            else resul = cuentaHojas(arbol.getIzq()) + cuentaHojas(arbol.getDer());

        return resul;
    }
    static int cuentaHojas (arbol a) {
        return cuentaHojas (a.getRaiz());
    }

    static void clavesNiveles (nodoArbol arbol, int n){
        if (arbol != null) {
            System.out.println("Clave: " + arbol.getClave() + " en el nivel: " + n);
            clavesNiveles (arbol.getIzq(), n+1);
            clavesNiveles (arbol.getDer(), n+1);
        }
    }*/
}
